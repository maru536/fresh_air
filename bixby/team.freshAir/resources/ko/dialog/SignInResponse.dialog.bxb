dialog (Result) {
  match {
    signInResponseConcept (response.code)
  }
  
  if (code == 200) {
    template ("로그인에 성공했습니다!")
  }
  else-if (code == 401 || code == 404) {
    template ("계정명/비밀번호를 확인하세요.")
  }
  else {
    template ("로그인에 실패했습니다...")
  }
}
